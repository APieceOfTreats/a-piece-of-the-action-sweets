const cart=[];const items=document.getElementById("items"),empty=document.getElementById("empty"),total=document.getElementById("total"),checkout=document.getElementById("checkout"),toast=document.getElementById("toast");document.getElementById("year").textContent=new Date().getFullYear();const money=n=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(n);function render(){items.innerHTML="";empty.style.display=cart.length?"none":"block";let t=0;cart.forEach((x,i)=>{t+=x.price*x.qty;const d=document.createElement("div");d.className="item";d.innerHTML=`<div><b>${x.name}</b><div>${money(x.price)} each</div></div><div class="qty"><button data-a="minus" data-i="${i}">−</button><span>${x.qty}</span><button data-a="plus" data-i="${i}">+</button></div><button class="remove" data-a="remove" data-i="${i}">Remove</button>`;items.appendChild(d)});total.textContent=money(t);checkout.disabled=!cart.length}document.querySelectorAll(".choices button").forEach(b=>b.onclick=()=>{const n=b.dataset.name,p=+b.dataset.price,f=cart.find(x=>x.name===n);f?f.qty++:cart.push({name:n,price:p,qty:1});render();toast.textContent=n+" added to your order";toast.classList.add("show");setTimeout(()=>toast.classList.remove("show"),1500)});items.onclick=e=>{const b=e.target.closest("button[data-a]");if(!b)return;const i=+b.dataset.i,a=b.dataset.a;if(a==="plus")cart[i].qty++;if(a==="minus"&&--cart[i].qty<=0)cart.splice(i,1);if(a==="remove")cart.splice(i,1);render()};render();